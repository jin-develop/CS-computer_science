js 함수

함수는 기본적으로 한개의 동작만 해야한다.

getAge라 하면 age를 받아오는 동작만해야하고, alert를 띄어주면 안된다.
createForm함수는 form을 만들고 반환하는 동작만해야하고, form을 문서에 추가하는 동작이 들어가면 안된다
checkPermission은 승인여부를 확인하고 그 결과를 반환하는 동작만 해야한다, 메세지를 띄우는 동작이 들어가 있음 안됌


만약에 함수가 길다면 함수를 잘게 쪼개야 한다.

